{% extends 'base.html.twig' %}

{% block title %}Create Allocation{% endblock %}

{% block body %}
    <h1>Create Allocation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Create</button>
    {{ form_end(form) }}

    <a href="{{ path('app_allocation_index') }}">Back to list</a>
{% endblock %}
